learn how to test



A user has-many dances
A user has-many choreographers
A choreographer is a string and it has many dances
A dance has one user, one choreographer, one start-type
(e.g. improper), up to 8 figures (encoded as strings), and some notes (text)


users may copy another user's dance ("fork"). This also adds the
choreographer to that user's choreographer table, if not already present

Grep through for comments with 'XXX' and resolve them

build a production server


